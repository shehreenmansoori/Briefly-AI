# ── Briefly AI ── requirements.txt ───────────────────────────────────────────
# Lightweight, Zero-PyTorch build optimized for Render 512MB RAM free tier

# ── Audio manipulation ────────────────────────────────────────────────────────
pydub>=0.25.1                     # Audio file manipulation / format conversion
gTTS>=2.5.0                       # Demo sample generator (pure python, lightweight)

# ── Fast Cloud Inference (Groq LPU: STT + LLM) ────────────────────────────────
groq>=0.9.0                       # Cloud Whisper API (whisper-large-v3-turbo)
langchain-groq>=0.1.0             # Fast generation & streaming (openai/gpt-oss-120b)

# ── Cloud Embeddings (Mistral API) ────────────────────────────────────────────
langchain-mistralai>=0.1.0        # mistral-embed embeddings
mistralai>=0.4.0                  # Mistral Python client

# ── LangChain Core & Orchestration ────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
pydantic>=2.7.0

# ── Streamlit UI & Utilities ──────────────────────────────────────────────────
streamlit>=1.35.0
python-dotenv>=1.0.0
requests>=2.32.0